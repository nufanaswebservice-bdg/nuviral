{
  "build": {
    "dockerfilePath": "../docker/Dockerfile.worker",
    "dockerContext": ".."
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "numReplicas": 1
  }
}
